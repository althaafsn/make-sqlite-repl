{
  "name": "make_codebase",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix web",
    "build": "npm run build --prefix web",
    "preview": "npm run preview --prefix web",
    "lint": "npm run lint --prefix web",
    "install:web": "npm install --prefix web",
    "deploy:aws": "npm run build && aws s3 sync web/dist/ s3://${AWS_S3_BUCKET} --delete && aws cloudfront create-invalidation --distribution-id ${AWS_CLOUDFRONT_DISTRIBUTION_ID} --paths '/*'"
  }
}
